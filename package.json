{
  "name": "iron-tide",
  "private": true,
  "description": "Unit tests for Iron Tide's pure game logic. The game itself has no build step — this is dev tooling only.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
